# Session Secret (CRITICAL - must be at least 32 characters)
# Generate with: openssl rand -base64 32
# This is used to encrypt session cookies
SESSION_SECRET=your-secret-here-must-be-at-least-32-characters

# Admin Password
# RECOMMENDED: Use bcrypt hash. Generate with: node scripts/hash-password.js YourPassword
# MINIMUM (if plain text): 12 characters, mixed case, numbers, special characters
# Example bcrypt hash: $2b$10$abcdefghijklmnopqrstuvwxyz123456789
ADMIN_PASSWORD=your-hashed-or-strong-password

# Viewer Password (for family/friends to access the site)
# RECOMMENDED: Use bcrypt hash. Generate with: node scripts/hash-password.js YourPassword
# MINIMUM (if plain text): 12 characters, mixed case, numbers, special characters
VIEWER_PASSWORD=your-hashed-or-strong-password

# Vercel Blob Storage
# Get your token from https://vercel.com/dashboard/stores
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxxxx

# Node Environment
NODE_ENV=development
